universe = vanilla
executable = /home/wfarr/PISNLineCosmography/fit.py
getenv = True

request_cpus = 4

nsamp = 400

output = logs/5yr_$(nsamp).out
error = logs/5yr_$(nsamp).err
log = logs/5yr_$(nsamp).log

arguments = --samp $(nsamp) --five-years

queue

nsamp = 800

output = logs/5yr_$(nsamp).out
error = logs/5yr_$(nsamp).err
log = logs/5yr_$(nsamp).log

arguments = --samp $(nsamp) --five-years

queue
